spring:
  application:
    name: vehicle_rra

  servlet:
    multipart:
      enabled: true
      max-file-size: 500MB
      max-request-size: 500MB

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/vehicle_rra
    username: postgres
    password: yezu



  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

jwt:
  secretKey: "xK29aP5rL8vQ6sW3zY1bN7cM4dF0eJoUiHkTfgAShXmZ"

server:
  port: 8844

uploads:
  extensions: pdf,png,jpeg,jpg
  directory: { user_profiles: /opt/ne-springboot/uploads/user_profiles, docs: /opt/ne-springboot/uploads/docs }
